pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri('../../usbHostForAndroid/build/repository') }
    }
}

rootProject.name = 'usb-host-consumer-smoke'
include ':consumer'
project(':consumer').projectDir = file('consumer')
